# Cheatsheet
## Common commands for quick reference

- Wazuh functions are stored in /var/ossec/bin
  - Manager-side /bin/ functions:
   1. /var/ossec/bin/manage_agents <- <div style='text-color: blue'>Intuitive agent access functionality</div>
   2. /var/ossec/bin/agent_control <- <div style='text-color: blue'>Critical flag-based tooling; <b>Valuable for Automation</b></div>
 
  - Agent-side /bin/ functions:
 
## Important files

- Includes logs and config
  - Agent-side config:
   1. /var/ossec/etc/ossec.conf <- <div style='text-color: blue'>Configure agent</div>

  - Logs:
   1. /var/ossec/logs
